// const arr = [1, 2, 3, 'hello', 'world', 'sdfsdg']

// function arrFilter(array) {
//    return array.filter(function(elem){
//       return typeof elem === 'number'
//    })
// }
// console.log(arrFilter(arr));

// ---------------------------------------------------------
// const arr = [-3, -5, 4, 7, 'hello', 'bot', 'sun']

// function arrFilter(array) {
//    return array
//       .filter((elem) => typeof elem === 'number')
//       .filter((elem) => elem > 0)
//       .map((elem) => elem ** 2)
// }
// console.log(arrFilter(arr));

// ---------------------------------------------------
// function multiply(a, b){
//    return a * b;
// }
// console.log(multiply(2, 3));

// -----------------------------------------------------------------
// const numbers = [1, 12, -54, -3, 32, 'germany', 'spain']

// function findSumOfPositiveElements(array) {
//    return array
//       .filter((elem) => typeof elem === 'number' && elem > 0)
//       .reduce((prev, current) => prev + current)
// }
// console.log(findSumOfPositiveElements(numbers));

// ---------------------------------------------------
// function maxAndMin(array) {
//    const arr = array.split(" ");
//    const max = Math.max(...arr);
//    const min = Math.min(...arr);

//    return `${max} ${min}`
// }
// console.log(maxAndMin("1 2 3 4 5"));

// -----------------------------------------------------------------------
